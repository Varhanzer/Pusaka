<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.beneranindonesia.pusaka">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Mission.PosActivity"></activity>
        <activity android:name=".activities.Mission.CompassDirection" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB5RNAvvngIbxF38oVzQTAzLICIyBxqXyI" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".activities.Mission.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".activities.Mission.AnswerQuestion.AnswerQuestionActivity" />
        <activity android:name=".activities.Account.ForgotActivityReset" />
        <activity android:name=".activities.Account.ForgotActivityCode" />
        <activity android:name=".activities.Account.ForgotActivity" />
        <activity android:name=".activities.Mission.AnswerQuestion.StartAnswerMissionActivity" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.OnBoard.OnBoardingActivity" />
        <activity android:name=".activities.Account.login.view.LoginActivity" />
        <activity android:name=".activities.Account.register.RegisterActivity" />
        <activity android:name=".activities.Mission.Detail.MissionDetailActivity" />
    </application>

</manifest>